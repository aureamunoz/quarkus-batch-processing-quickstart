package org.acme.getting.started;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeBatchProcessingResourceIT extends BatchProcessingResourceTest {

    // Execute the same tests but in native mode.
}